import 'package:audio_chat/src/audio_data.dart';
import 'package:flutter/material.dart';

class AudioBubble extends StatelessWidget {
  const AudioBubble({Key? key, required this.data}) : super(key: key);

  final AudioData data;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
